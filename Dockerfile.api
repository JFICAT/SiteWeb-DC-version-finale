FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
# On installe uniquement les dépendances runtime (pas vite, tailwind, etc.)
RUN npm ci --omit=dev

COPY server/ ./server/

EXPOSE 3001

CMD ["node", "server/index.js"]
