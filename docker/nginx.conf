server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Resolver DNS interne Docker — résolution à la demande (pas au démarrage)
    resolver 127.0.0.11 valid=30s ipv6=off;

    # Proxy /api/* vers le conteneur Node (service "api" sur le réseau interne)
    location /api/ {
        set $api_upstream http://dataconforme-api:3001;
        proxy_pass         $api_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 10s;
    }

    # Routing SPA React : toutes les routes renvoient index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Sécurité : pas d'accès aux fichiers cachés
    location ~ /\. {
        deny all;
    }
}
